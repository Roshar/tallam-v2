# Database dumps (not committed to git)

Place your MySQL dump here, for example:

```
production.sql
```

Then run from project root:

```bash
npm run db:import -- database/dumps/production.sql
```

Or if the file is elsewhere:

```bash
npm run db:import -- ~/Downloads/govzalla_t_25_full.sql
```
